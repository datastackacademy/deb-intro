import pytest
from example_funcs import say_hi

# assert that say_hi() returns a string greeting the user when they pass in a string

# assert that say_hi() returns `None` when the user doesn't pass in an argument

# assert that say_hi() raises an exception when too many arguments are passed in

# assert that say_hi() can handle arguments that are data types other than strings

